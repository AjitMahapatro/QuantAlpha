# Model Artifacts

Persist trained model files, evaluation reports, and experiment metadata here when you want artifacts outside `backend/models/`.

The backend currently writes XGBoost artifacts to `backend/models/` by default.
